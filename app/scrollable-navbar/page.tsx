import MainPage from '@/components/(navbar-hide-scroll)/MainPage'
import { FC } from 'react'

interface pageProps {}

const page: FC<pageProps> = ({}) => {
  return <MainPage />
}

export default page
